<UserControl x:Class="STranslate.Plugin.Translate.UApiPro.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:plugin="clr-namespace:STranslate.Plugin;assembly=STranslate.Plugin"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:vm="clr-namespace:STranslate.Plugin.Translate.UApiPro.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <!-- 主容器：SimpleStackPanel 垂直堆叠，Spacing=12 控制卡片间距 -->
    <ikw:SimpleStackPanel Spacing="12">

        <!-- ===== 卡片1：API Key（PasswordBox 直接绑定，无需代码后台） ===== -->
        <ui:SettingsCard Header="{Binding ApiKeyLabel}"
                         Description="{Binding ApiKeyDescription}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Key_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <PasswordBox MinWidth="300"
                         plugin:PasswordBoxAssistant.Attach="True"
                         plugin:PasswordBoxAssistant.Password="{Binding ApiKey}"
                         ui:ControlHelper.PlaceholderText="{Binding ApiKeyPlaceholder}" />
        </ui:SettingsCard>

        <!-- ===== 卡片2：官网（HyperlinkButton 超链接按钮） ===== -->
        <ui:SettingsCard Header="{Binding WebsiteLabel}"
                         Description="{Binding WebsiteDescription}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Globe_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ui:HyperlinkButton Content="https://uapis.cn"
                                NavigateUri="https://uapis.cn" />
        </ui:SettingsCard>

        <!-- ===== 卡片3：校验（AccentButton 样式 + IconAndText） ===== -->
        <ui:SettingsCard Header="{Binding VerifyLabel}"
                         Description="{Binding VerifyDescription}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.CheckmarkCircle_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="12">
                <Button Command="{Binding TestCommand}"
                        Style="{StaticResource AccentButtonStyle}">
                    <ui:IconAndText Content="{Binding TestButtonText}"
                                    Icon="{x:Static ui:FluentSystemIcons.Play_24_Regular}" />
                </Button>
            </ikw:SimpleStackPanel>
        </ui:SettingsCard>

    </ikw:SimpleStackPanel>
</UserControl>
